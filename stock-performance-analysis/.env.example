# Copy this file to `.env` and fill in your real key. `.env` is gitignored — never commit real secrets.
ANTHROPIC_API_KEY=sk-ant-api03-...

# Optional: override the Claude model used by all three agents (default: claude-sonnet-4-5)
# CLAUDE_MODEL=claude-sonnet-4-5
